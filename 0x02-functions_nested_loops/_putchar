#include <unistd.h>

/**
*_putchar - writes the character c to stdout
*@c: The character to print
*
*Return: On succession 1.
*On error, -1 is returne, and ernno is set appropriately.
*/

int _putchar(char c)
{
return(write(1, &c, 1));
}
